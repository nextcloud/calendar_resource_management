{
  "name": "calendar-resource-management",
  "version": "1.0.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "build": "vite build --mode production",
    "dev": "vite build --mode development",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "stylelint": "stylelint \"src/**/*.vue\" \"src/**/*.scss\" \"src/**/*.css\" --allow-empty-input",
    "stylelint:fix": "stylelint \"src/**/*.vue\" \"src/**/*.scss\" \"src/**/*.css\" --fix --allow-empty-input",
    "watch": "vite build --mode development --watch"
  },
  "browserslist": [
    "extends @nextcloud/browserslist-config"
  ],
  "dependencies": {
    "@mdi/js": "^7.4.47",
    "@nextcloud/axios": "^2.5.1",
    "@nextcloud/dialogs": "^7.0.0",
    "@nextcloud/l10n": "^3.4.1",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@nextcloud/vite-config": "^2.5.2",
    "@vitejs/plugin-vue": "^6.0.3",
    "vite": "^7.3.1"
  },
  "engines": {
    "node": "^24.0.0",
    "npm": "^11.3.0"
  }
}
